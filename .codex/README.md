# Codex home

This versioned baseline contains non-secret Codex configuration and instructions.
Authentication and session data are ignored.

```bash
export CODEX_HOME=/workspace/devenv/.codex
codex
```

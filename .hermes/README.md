# Hermes home

This versioned baseline contains non-secret configuration and skills.

Ignored: API keys, OAuth tokens, sessions, logs, caches, and databases.

```bash
export HERMES_HOME=/workspace/devenv/.hermes
hermes setup
```
